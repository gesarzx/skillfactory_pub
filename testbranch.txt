Выполните команду git status и вы увидите сообщение о том, что вы находитесь на ветке testbranch00.
img

Выполним команду git log. Сейчас можно видеть, что индекс в локальном репозитории указывает на ветку testbranch00, а в удалённом — на ветку master. Давайте сделаем изменения в локальном репозитории, находясь в данный момент на ветке testbranch00.
Задание 9.5.1
Добавьте какой-нибудь (лучше текстовый) файл в репозиторий, добавьте его в индекс и зафиксируйте изменения. Синхронизировать с удалённым сервером пока не нужно.

Для проверки выполните команду git status после каждого шага.

Выполните команду git log и вы увидите, что на самый поздний коммит указывает индекс на ветке testbranch00. Следующий коммит — это коммит на удалённом сервере.

Выполните команду операционной системы dir и запомните файлы, находящиеся в репозитории. Обратите внимание, что файл, который вы только что создали, находится в репозитории